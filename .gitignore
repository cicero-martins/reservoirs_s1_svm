# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Large raw data / binaries
*.zip
raw_data/HydroLAKES_polys_v10_shp/
validation_data/DAHITI/
validation_data/GEEvalidation-extracted/
validation_data/pilot/
validation_data/GROWL_Timeseries.zip
validation_data/GRDL.zip
validation_data/GEEvalidation2-*.zip
validation_data/HydroLAKES_pilot24_shp/
validation_data/HydroLAKES_pilot24.zip

# GEE raw exports (regenerable from exportGlobalPilot.js)
validation_data/GROWL_SAR_pilot/

# PDFs (too large, stored separately)
references/*.pdf
submission/*.pdf

# Figures (regenerable from scripts)
validation_data/**/*.png
validation_data/ablation_*.png

# Secrets / credentials (AEGIS CFD Sicilia API tokens — never commit)
analysis/_aegis_token.json
analysis/_aegis_elements*.json
*_token.json
*.token

# Scratch / probe / diagnostic scripts (underscore-prefixed, throwaway)
analysis/_probe_*.py
analysis/_diag_*.py
analysis/_vol_table.py

# Office lock files
**/~$*

# Local IDE
.vscode/
*.code-workspace

# LaTeX build artifacts (regenerable from .tex)
manuscript/*.aux
manuscript/*.bbl
manuscript/*.blg
manuscript/*.fdb_latexmk
manuscript/*.fls
manuscript/*.log
manuscript/*.out
manuscript/*.spl
manuscript/*.synctex.gz
manuscript/main.pdf

# GEE raw exports (regenerable from the export scripts; large)
raw_data/GEE_*/
raw_data/GDW2024/
raw_data/ee-chart_*.csv

# Throwaway diagnostic logs
analysis/_*_log.txt
